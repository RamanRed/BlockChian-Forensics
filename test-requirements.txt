# ── Test Dependencies ────────────────────────────────────────
# Run: uv venv test_env && uv pip install -r test-requirements.txt

pytest>=8.2.0
pytest-asyncio>=0.23.7
pytest-cov>=4.1.0
httpx>=0.27.0
pillow>=10.3.0
torch>=2.3.0
transformers>=4.38.2
