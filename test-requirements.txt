# ── Test Dependencies ────────────────────────────────────────
# Run: uv venv test_env && uv pip install -r test-requirements.txt

# ── Testing Framework ────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.7
pytest-cov>=4.1.0
httpx>=0.27.0

# ── Backend Dependencies (needed for imports) ──────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.6
sqlalchemy>=2.0.30
pydantic[email]>=2.7.1
python-dotenv>=1.0.1
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
web3>=6.18.0

# ── Async I/O ────────────────────────────────────────────────
aiohttp>=3.9.5
aiofiles>=23.2.1

# ── AI / Deep Fake Detection ─────────────────────────────────
pillow>=10.3.0
numpy>=1.26.4
torch>=2.3.0
transformers>=4.38.2
