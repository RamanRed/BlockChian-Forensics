# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.1                    # database migrations

# ── Validation & Config ───────────────────────────────────────────────────────
pydantic[email]==2.7.1
python-dotenv==1.0.1

# ── Authentication ────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9            # form/file uploads

# ── Async I/O ─────────────────────────────────────────────────────────────────
aiohttp==3.9.5
aiofiles==23.2.1

# ── Blockchain ────────────────────────────────────────────────────────────────
web3==6.18.0

# ── AI / Image processing ─────────────────────────────────────────────────────
transformers==4.38.2          # Hugging Face transformers (ViT for deepfake detection)
torch==2.3.0                  # PyTorch – CPU version
# For GPU: pip install torch==2.3.0 --index-url https://download.pytorch.org/whl/cu121
pillow==10.3.0
numpy==1.26.4
# tensorflow or older torch – commented out (using transformers + torch instead):
#   pip install tensorflow-cpu==2.16.1

# ── Dev / Testing (optional) ──────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0                    # async test client for pytest
